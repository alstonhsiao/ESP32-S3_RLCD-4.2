#pragma once
// Copy to secrets.h and fill in (secrets.h is gitignored).
// If left as placeholders, firmware falls back to WiFiManager setup AP.

#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASS "YOUR_WIFI_PASSWORD"
