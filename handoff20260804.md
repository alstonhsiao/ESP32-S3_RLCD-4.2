# Handoff — 2026-08-04

給明天（或下一個 session）接著做。使用者已累，**今天到此為止**。

---

## 一句話狀態

Waveshare **ESP32-S3-RLCD-4.2** 專案已鎖定 **Arduino + U8g2**，螢幕 Hello OK，**aiusage 多頁儀表（P0/P1/P2）已燒錄並由使用者確認可用**（含 BOOT 翻頁）。

---

## 硬體

| 項目 | 值 |
| --- | --- |
| 板子 | Waveshare ESP32-S3-RLCD-4.2 |
| 螢幕 | 4.2" RLCD ST7305，300×400 / 邏輯 400×300，無背光 1bpp |
| 本機 USB | 曾用 `/dev/cu.usbmodem11401`（`arduino-cli board list` 再確認） |
| 晶片 | ESP32-S3 + 8MB PSRAM，MAC `a4:cb:8f:d0:6c:00` |

腳位與 SPEC：`docs/specs/`（含 schematic PDF、ST7305 datasheet）。

---

## 框架與工具（本機）

- **已鎖定**：Arduino + U8g2（≥ 2.36.19）
- `arduino-cli` 1.5.0，`esp32:esp32` 3.3.7
- 函式庫：U8g2、ArduinoJson、WiFiManager
- **FQBN**：
  ```text
  esp32:esp32:esp32s3:CDCOnBoot=cdc,PartitionScheme=huge_app,FlashSize=16M,PSRAM=opi
  ```

```bash
FQBN='esp32:esp32:esp32s3:CDCOnBoot=cdc,PartitionScheme=huge_app,FlashSize=16M,PSRAM=opi'
PORT='/dev/cu.usbmodem11401'   # 以 board list 為準

arduino-cli compile --fqbn "$FQBN" --libraries "$HOME/Documents/Arduino/libraries" firmware/aiusage_home
arduino-cli upload -p "$PORT" --fqbn "$FQBN" firmware/aiusage_home
arduino-cli monitor -p "$PORT" -c baudrate=115200
```

---

## 今天完成

1. **SPEC 基建** — `docs/specs/`、`AGENTS.md`、`README.md`
2. **參考** — [ClaudeSlate](https://github.com/HarryXin0919/ClaudeSlate)（同硬體、Arduino+U8g2、BOOT 翻頁、proxy 模式）
3. **Wireframe** — `ui/aiusage-wireframe.html`（P0–P2 + empty 狀態，使用者說不用改）
4. **Hello RLCD** — `firmware/hello_rlcd/` 燒錄成功，螢幕 OK
5. **aiusage 儀表** — `firmware/aiusage_home/`
   - `GET https://aiusage-web.zeabur.app/data`
   - 語意：**週剩餘 % = 100 − used_weekly_pct**
   - **P0 Home** 時鐘 + 四源
   - **P1 Detail** WEEK / 5H / RESET，低剩餘反白
   - **P2 Trend** 最近最多 40 點折線
   - **短按 BOOT** 翻頁；**長按 3s** WiFi 配網（`AIUsage-RLCD`）
   - 使用者回報：**完成了**（功能可用）

---

## 操作備忘（給使用者）

| 操作 | 說明 |
| --- | --- |
| 換頁 | 短按 **BOOT**（靠 USB 側鍵，不是 PWR） |
| 重配網 | 長按 BOOT ~3 秒 → 手機連 `AIUsage-RLCD` → `192.168.4.1` |
| WiFi | 僅 **2.4 GHz**；配過後 WiFiManager 會記在 NVS |
| 寫死 WiFi | 複製 `secrets.h.example` → `secrets.h`（已 gitignore） |
| 看螢幕 | 需環境光（無背光） |

---

## 重要路徑

```text
AGENTS.md                          # agent 約定與進度
README.md                          # 專案說明
handoff20260804.md                 # 本檔
docs/specs/                        # 硬體 SPEC + PDF
ui/aiusage-wireframe.html          # 單色設計稿
firmware/hello_rlcd/               # 最小顯示驗證
firmware/aiusage_home/             # 主功能 sketch（目前在板子上）
```

資料源：

- Web UI：https://aiusage-web.zeabur.app/
- API：`GET /data`、`GET /health`
- 相關本機專案（ingest 來源，非本 repo）：`keyboardMaestro/autousage`

---

## 明天建議接著做（優先序）

不必全做，依精力選：

1. **UX 打磨**
   - P1 四列在 300 高是否擁擠 → 微調間距/字級
   - P2 四線重疊可讀性 → 可改「單源切換」或只畫 2 源
   - 軟體反顯（黑字白底）若對比不理想，可參考 ClaudeSlate `INVERT_DISPLAY`

2. **穩定度**
   - HTTPS / 大 JSON（~60KB）記憶體與 timeout
   - 離線/stale 狀態文案與重試
   - 可選：後端加精簡 API（最新 + N 點）減輕裝置負擔

3. **功能擴充**
   - 板載 **SHTC3** 溫濕度顯示（ClaudeSlate 底欄 Room）
   - **KEY**（GPIO18）第二操作（例如只刷資料、或鎖定某頁）
   - 天氣（Open-Meteo）或 ClaudeSlate 式本機 proxy

4. **工程**
   - 若尚未 push：確認 GitHub remote、`secrets.h` 未進版控
   - 考慮 `firmware/aiusage_home` 拆檔（ui / net / data）

---

## 刻意未做 / 不做假設

- 未在螢幕上「開瀏覽器」渲染網頁（技術上不可行）
- 未上 ESP-IDF / ESPHome 主線
- 未做語音 / 喇叭
- 未提交 `firmware/**/secrets.h`

---

## 復原檢查清單（明天開機）

1. 讀本 handoff + `AGENTS.md` §7  
2. USB 插板 → `arduino-cli board list`  
3. 看螢幕是否仍為 aiusage（或重燒 `firmware/aiusage_home`）  
4. 短按 BOOT 確認 P0→P1→P2  
5. 再決定 UX 或感測擴充  

---

*寫於 2026-08-04 深夜，session 結束 handoff。*
